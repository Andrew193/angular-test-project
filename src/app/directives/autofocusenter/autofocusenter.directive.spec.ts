import { AutofocuscenterDirective } from './autofocusenter.directive';

describe('AutofocusenterDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofocuscenterDirective();
    expect(directive).toBeTruthy();
  });
});
